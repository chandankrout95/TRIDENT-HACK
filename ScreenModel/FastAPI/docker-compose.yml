services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    # This command points to the app object inside api/app.py
    command: uvicorn api.app:app --host 0.0.0.0 --port 8000 --reload